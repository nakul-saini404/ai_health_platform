# ── HeartGuard AI — Requirements ─────────────────────────────────────────────
# Install with: pip install -r requirements.txt

# Web framework
flask>=2.3.0

# ML & data
scikit-learn>=1.3.0       # Heart disease model (Pipeline + RandomForest)
joblib>=1.3.0             # Loading .pkl model
numpy>=1.24.0
pandas>=2.0.0

# Brain tumor model (MobileNet .h5)
tensorflow>=2.15.0        # Loads brain_tumor_mobilenet.h5 via keras
Pillow>=10.0.0            # Image preprocessing for MRI scans

# ── Optional but recommended ──────────────────────────────────────────────────
gunicorn>=21.0.0          # Production server (instead of Flask dev server)
